```
# Role & Context
You are a Senior Full-Stack Game Engineer. We have validated our Phaser 3 + TypeScript MVP. Now, according to our technical design spec, we must transition from the local mock controller to a **REAL LLM connection with deep-seek/openai architecture** and introduce the **Survival Game Loop (Global Resource System)**.

Please directly update and create the necessary files in our `"d:\Prompt创世纪"` project based on the requirements below.

---

# Task 1: Update Type Contracts (`src/types.ts`)
Update the TypeScript interfaces to enforce strict data symmetry between the AI's reasoning and our Phaser 3 game state updates:
​```typescript
export type NPCName = '阿强' | '阿珍' | '阿衰';
export type NPCAction = 'WALK_TO' | 'PLAY_ANIMATION' | 'SPEAK' | 'IDLE';
export type AnimationType = 'CHOP' | 'READ' | 'SLEEP' | 'IDLE';

export interface NPCCommand {
  npcName: NPCName;
  action: NPCAction;
  targetX?: number;
  targetY?: number;
  animationType?: AnimationType;
  bubbleText?: string;
}

export interface LLMResponse {
  eventName: string;
  commands: NPCCommand[];
  stateImpact: {
    woodDelta: number;      // Resource change: e.g., +1, +2
    knowledgeDelta: number; // Resource change: e.g., +1
    hungerDelta: number;    // Collective energy drain: e.g., -15
  };
}

export interface GlobalGameState {
  wood: number;
  knowledge: number;
  hunger: number;          // 0 - 100
  timeLeft: number;        // Countdown in seconds (60s)
  isGameOver: boolean;
}
```

# Task 2: Implement Node.js Lightweight Proxy Server (`server.js`)

To protect our API key and bypass CORS, scaffold a minimal backend router inside the project (you can use Express or a vanilla `http` server since we are in Vite).

- Read the API key from a `.env` file via `process.env.DEEPSEEK_API_KEY`.
- Endpoint: `POST /api/divine-command`
- Use the standard OpenAI/DeepSeek SDK format (or simple native `fetch`) to send the payload.
- **CRITICAL: Inject the Anti-Hallucination System Prompt**:

Plaintext

```
你是一个 2D 沙盒游戏《Prompt创世纪》的后台神明意志解析引擎。
当前场景包含 3 个 NPC：
1. 阿强 (擅长砍树), 2. 阿珍 (擅长读书研究), 3. 阿衰 (擅长在竹榻睡觉恢复体力)

游戏地图固定资源坐标（像素位置）为：
- 🌲 古松森林: [600, 150] (如果让阿强去砍树，请分发 WALK_TO 且目的地为此坐标)
- 📚 岭南书斋: [160, 360] (如果让阿珍去读书，请分发 WALK_TO 且目的地为此坐标)
- 💤 竹榻营地: [400, 540] (如果让阿衰去躺平/休息，请分发 WALK_TO 且目的地为此坐标)

玩家会输入任意的“神谕指令”（自然语言）。你的任务是：
1. 理解玩家意图，合理拆解为这三个 NPC 的行动队列，并科学演算本次行动对全局资源（woodDelta, knowledgeDelta, hungerDelta）的影响。如果是符合他们特长的行为（如阿强砍树），资源增加更多，但任何行动都会消耗一定饱食度（hungerDelta 为负数）。如果让他们去休息（如阿衰躺平），hungerDelta 可以为正（恢复饱食度）。
2. 必须且只能输出符合 TypeScript 接口 `LLMResponse` 的纯 JSON 对象。绝对不能包含任何多余的解释、不要 Markdown 标记（严禁使用 ```json 这种包裹包裹包裹）。
```

# Task 3: Integrate Game Loop & Survival UI into Frontend

Modify `src/main.ts` and `src/GameScene.ts` to manage the ticking survival clocks:

1. **Global State Integration:** Initialize the game state: `wood: 0, knowledge: 0, hunger: 100, timeLeft: 60`. Render a premium HUD at the top of the browser using Tailwind CSS:
   - **Left:** 🌲 木材: [X]/3 | 📚 知识: [X]/3
   - **Center (BIG):** ⏳ 距离大洪水降临: [X] 秒
   - **Right:** 🍖 信徒集体饱食度: [X]% (Use a Tailwind colored progress bar)
2. **The Ticking Clock:** Use a standard `setInterval` or Phaser Timer Event to decrement `timeLeft` by 1 every second.
   - Every 5 seconds of idle game time, decrement `hunger` by 2%.
   - If `timeLeft === 0`, trigger settlement: If `wood >= 3 && knowledge >= 3` -> trigger WIN MODAL. Else -> trigger LOSE MODAL.
   - If `hunger <= 0` -> instantly trigger LOSE MODAL ("信徒已全饿死").
3. **Wire Button to Real API:** When the "降下神谕" button is clicked:
   - Disable input, change button text to "神明正在思考中(LLM Reasoning)..."
   - `fetch('/api/divine-command')` sending the text input.
   - On response: parse JSON safely (add a `try/catch` guard to default to `IDLE` if LLM halucinates).
   - Apply `stateImpact` immediately to the UI counters.
   - Pass `commands` array to `scene.executeAICommands()` to make sprites move/speak.

Please write out the code modifications neatly and update the files. Ensure TypeScript types compile flawlessly. Let's make this engine alive!