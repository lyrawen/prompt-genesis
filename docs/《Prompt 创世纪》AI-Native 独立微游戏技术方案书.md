# 《Prompt 创世纪》AI-Native 独立微游戏技术方案书

## 一、 项目背景与核心核心定位

在传统游戏开发中，AI 通常仅作为 NPC 对话的辅助配件，移除 AI 并不影响游戏核心逻辑流转。 本工程旨在构建一款**完全以 AI 为核心驱动（AI-Native）的 2D 像素风沙盒求生微游戏原型**。游戏摒弃了传统的 UI 按钮点选操作，将**自然语言（Prompt）作为唯一的玩法输入（Gameplay）**。大模型不仅扮演剧情策划，更直接充当**游戏控制器（Controller）与状态演算引擎**。

项目整体设计对标 **Playable Ads（可玩广告）** 标准：具备**轻量化、低延迟、强交互、反馈极快**的商业微游戏特征。

## 二、 核心玩法与生存循环（Game Play Loop）

游戏单局时间为 **60 秒倒计时**，玩家扮演“神明”，需要在一场“末日大洪水”来临前，通过输入“神谕（Prompt）”指导小岛上的 3 个像素信徒合作求生。

### 1. 角色设定与资源矩阵

- **信徒资产：**
  - **阿强**（擅长砍树，伐木产出翻倍，但消耗饱食度高）
  - **阿珍**（聪明书生，擅长在书斋研究，产出文明知识）
  - **阿衰**（慵懒村民，擅长在竹榻睡觉恢复体力，干活效率极低）
- **全局数值：** 【木材数值】、【知识数值】、【集体饱食度（100% 递减）】。

### 2. 核心玩法逻辑闭环

```
 ┌────────────────────────────────────────────────────────┐
 │                      玩家输入神谕                       │
 └──────────────────────────┬─────────────────────────────┘
                            ▼
 ┌────────────────────────────────────────────────────────┐
 │             云端大模型 (LLM Tool Calling)              │
 │   1. 语义拆解为行动指令 (JSON)   2. 演算对游戏数值的影响  │
 └──────────────────────────┬─────────────────────────────┘
                            ▼
 ┌────────────────────────────────────────────────────────┐
 │                  Phaser 3 游戏引擎                     │
 │   1. 异步指令排队执行         2. 触发动画、寻路、流汗粒子 │
 └──────────────────────────┬─────────────────────────────┘
                            ▼
 ┌────────────────────────────────────────────────────────┐
 │                 大洪水临界结算 (60s)                    │
 │   成功：木材>=3 & 知识>=3 -> 【文明延续】               │
 │   失败：任一死亡或资源不足 -> 【末日湮灭】               │
 └────────────────────────────────────────────────────────┘
```

## 三、 核心技术架构设计（面试核心技术亮点）

为了解决“游戏引擎的**高帧率同步渲染**”与“大模型**长延迟异步 I/O**”之间的天然冲突，本系统设计了以下三大技术架构亮点：

### 1. 异步 I/O 解耦与事件驱动（Event-Driven Architecture）

- **痛点：** 大模型请求需要 0.5s ~ 2s，若直接阻塞游戏主循环（Update Loop）会导致彻底卡死。
- **解耦方案：** 前端游戏引擎保持 60 FPS 稳定渲染，玩家点击“降下神谕”后，UI 触发“神明思考中”的状态动画。网络请求在 Loop 之外通过 `fetch` 异步处理，得到大模型严格的 `LLMResponse` JSON 契约后，通过事件派发器（Event Emitter）一次性注入 Phaser 场景，实现视觉与数据的平滑同步。

### 2. 严格的类型契约与确定性边界限制（Anti-Hallucination Guardrail）

- **痛点：** LLM 容易产生“幻觉”，输出无法预知的文本或违反游戏规则的指令。
- **架构防线：**
  - **大模型侧：** 开启大模型的 `Structured Outputs / JSON Mode`。通过极致压缩的 System Prompt 约束其输出，强制要求其充当“指令翻译器”。
  - **前端侧：** 利用 TypeScript 构建严格的 `NPCCommand` 契约体系。前端接收到 JSON 后，先通过状态机校验（如：在思考状态才可移动）。任何大模型越界的输出直接被捕获并降级为 `IDLE`（原地待机），确保游戏逻辑绝对不会崩溃。

### 3. 多模态映射：数据驱动的视听反馈

- 大模型传回的结构化数据（如：NPC 的心理紧张度、饱食度、行动意图）不仅改变数值，还会直接动态映射到前端的**视觉表现层**。
- 例如：饱食度低于 20% 时，Phaser 引擎通过动画管理器（Animation Manager）自动将 NPC 的移动速度降半，并在头顶持续触发流汗的粒子特效，实现“数据驱动视觉”的现代游戏开发原则。

## 四、 商业加分项落地：AIGC 美术与技术闭环（Vibe Coding 的体现）

作为一名现代全栈/Web 游戏开发者，本项目践行了高效的 **“AI 协同生产流”**：

1. **代码全量人机协同：** 项目骨架、网格寻路算法、异步 Promise 队列链式执行等核心代码，均由开发者提供宏观架构架构设计，利用 **Cursor / Claude Code** 敏捷卷出（Vibe Coding Mode），开发效率较传统独立开发提升 400%。
2. **视觉资产 AIGC 化：**
   - **设计概念：** 采用“新中式数字美学”像素风。
   - **视觉落地方案：** 游戏内的地形（古松森林、岭南书斋、竹榻营地）和 3 个小人角色，全量使用 **Midjourney** 设定国风 prompt 批量生成，再通过脚本自动转为 8-bit/16-bit 像素图，切分为 Phaser 3 识别的**雪碧图（Sprite Sheet）**，实现零成本、高品质的美术资产闭环。